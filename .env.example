# Copy this file to ".env" and fill in your values, then run ./run_live.sh
# (.env is gitignored — your secrets never leave the Mac.)

# --- which real market to stream live bid/ask from -----------------------
# Run `python check_exchange.py` first to confirm it connects from YOUR line.
# kucoin: lists all 5 coins (matches Goat's BTC/ETH/SOL/BNB set), reachable
#   for public data, good proxy for the funded feed -> recommended for testing.
# kraken: UK-legal/reliable but no BNB (engine skips it).
# Bybit/OKX geo-block the UK -> don't use them there.
EXCHANGE=kucoin

# --- which coins to trade ------------------------------------------------
# BTC,ETH,SOL is the lineup validated against Goat's REAL spreads (LTC's wide
# ~38bps spread makes it a net loser; BNB is marginal). Add them back only on
# a genuinely tight venue:  COINS=BTC,ETH,SOL,BNB,LTC
COINS=BTC,ETH,SOL

# --- Telegram phone notifications (free) ---------------------------------
# 1) message @BotFather -> /newbot -> copy the token
# 2) message your new bot once, then open
#    https://api.telegram.org/bot<TOKEN>/getUpdates and copy the chat id
TELEGRAM_TOKEN=
TELEGRAM_CHAT=

# --- cloud sync so the hosted dashboard sees your Mac's fills ------------
# GITHUB_TOKEN: a token with ONLY the "gist" scope (github.com/settings/tokens)
# GIST_ID:      create once with:  GITHUB_TOKEN=... python cloud_sync.py
GITHUB_TOKEN=
GIST_ID=
